# 0611課程筆記

建立時間: 2026年6月11日 下午7:05
更新時間: 2026年6月11日 下午7:16
有實作: No

# GCP與AWS對應的經典架構差別

以下分為「專案管理」**、**「計費帳戶架構」**與**「MFA（多因素驗證）」三大維度來回答：

## 1. 資源與專案管理架構的差別 (Project vs. Account)

- **GCP (以「專案 Project」為核心)：**
    - 在 GCP 中，**所有資源（如 GCE 虛擬機、VPC、資料庫）都必須隸屬於某一個「專案（Project）」**。
    - **API 啟用機制 (Enable APIs)：** 這是 GCP 最特殊的架構。新專案建立時，預設所有的雲端服務（如 Compute Engine API）都是關閉的。你**必須手動或透過腳本「啟用 (Enable)」該服務的 API**，系統才會允許你在該專案內建立資源與計算費用。
- **AWS (以「帳戶 Account」為核心)：**
    - AWS 的隔離層級直接建立在「AWS 帳戶（Account）」本身。
    - 在 AWS 帳戶內，大部分核心服務（如 EC2、S3）預設就是開箱即用的，不需要像 GCP 一樣逐一去 Enable 特定的 API 才能開始用。

## 2. 計費帳戶架構的差別 (Billing Account)

- **GCP (計費帳戶與專案解耦)：**
    - GCP 的 **Billing Account（計費帳戶）** 是一個獨立的物件。
    - 一個計費帳戶可以同時綁定（Link）到多個不同的 Project 上。這意味著你可以隨時把 Project A 從帳戶甲移轉到帳戶乙，專案內的環境與資源完全不受影響。如果專案沒有綁定有效的 Billing Account，就無法啟動付費資源。
- **AWS (計費直接綁定帳戶)：**
    - 在 AWS 中，信用卡與付款資訊通常直接綁定在該 AWS 帳戶本身。
    - 如果要管理多個帳戶的帳單，AWS 是透過 **AWS Organizations（組織）** 的整合帳單（Consolidated Billing）功能，由一個 Master Account 來統一支付其他聯網帳戶的費用。

## 3. 帳號安全與 MFA 驗證的差別

- **GCP (依賴 Google 生態系的 2-Step Verification)：**
    - GCP 本身沒有獨立的「GCP 登入密碼」，它是直接沿用 **Google 帳號（個人 Gmail 或企業 Workspace 帳號）** 的安全機制。
    - 當你需要開啟 MFA（兩步驟驗證）時，你是去設定 Google 帳號的安全安全性，例如使用 Google 提示、身分驗證器（Google Authenticator）或實體金鑰。一旦帳號啟動了兩步驟驗證，登入 GCP 主控台時就會自動強制要求 MFA。
- **AWS (獨立的 IAM 與 Root MFA)：**
    - AWS 擁有自己獨立的帳號體系。它明確區分 **Root User（根帳號）** 與 **IAM User（身分與存取管理使用者）**。
    - 在 AWS 最佳實務中，強烈要求必須為 Root 帳號和每個 IAM 帳戶**單獨設定 AWS 內建的 MFA 虛擬裝置**（例如綁定 Authenticator APP 產生的動態 6 位數 Code）。AWS 的安全原則是「預設最低權限」，必須在 AWS IAM 介面中強制控管。

## 📊 快速總結對比表

| **比較項目** | **Google Cloud Platform (GCP)** | **Amazon Web Services (AWS)** |
| --- | --- | --- |
| **資源隔離核心** | **Project (專案)**：資源都關在專案裡。 | **Account (帳戶)**：以整個帳戶做隔離。 |
| **服務啟用** | 必須手動 **Enable API** 才能使用該服務。 | 預設大部分服務皆可直接使用。 |
| **計費架房 (Billing)** | **Billing Account** 獨立存在，可自由綁定/解綁多個專案。 | 帳單直接跟隨 **AWS 帳戶**，多帳戶透過 AWS Organizations 整合。 |
| **安全與 MFA** | 綁定 **Google 帳號** 的兩步驟驗證（2-Step Verification）。 | 由 **AWS IAM** 機制獨立控管，嚴格區分 Root 與 IAM User 的 MFA。 |

# GCP 的資源結構

GCP（Google Cloud Platform）的資源結構是採用類似檔案系統的「階層式組織架構」（Hierarchical Structure）。這種設計能讓企業非常方便地由上而下管理權限（IAM）和掌控帳單費用。

整個結構由上到下總共分為四個層級，就像一個倒過來的樹狀圖：

### 1. 組織 (Organization) —— 樹根

- **概念：** 這是結構的最頂層，通常代表整個「公司」或「學校機構」（例如 `yourcompany.com` 或 `yourschool.edu.tw`）。
- **作用：** 所有的資源、專案和計費都屬於這個組織。中央資訊主管可以在這裡設定全公司一體適用的資安政策（Organization Policies）。

### 2. 資料夾 (Folders) —— 樹幹與樹枝（選填）

- **概念：** 類似電腦裡的資料夾，用來對專案進行分類。
- **作用：** 你可以根據「部門」（如：研發部、市場部）或「環境」（如：測試環境 Production、開發環境 Development）來建立資料夾。權限可以直接設定在資料夾上，底下的專案會自動繼承。

### 3. 專案 (Projects) —— 樹枝

- **概念：** 資源管理的**核心基礎**（就像我們上課練習開的專案）。在 GCP 中，任何計算或儲存資源都**不能孤立存在**，必須被關在某一個專案裡。
- **作用：** * 專案是啟用 API、管理成員權限、以及綁定計費帳戶（Billing Account）的層級。
    - 專案彼此之間預設是完全隔離的。

### 4. 資源 (Resources) —— 樹葉

- **概念：** 結構的最底層，也就是你實際花錢建立並使用的雲端服務。
- **常見例子：** 你開的 GCE 虛擬機（VM）、儲存檔案的 Cloud Storage 雲端硬碟、VPC 網路防火牆、或是資料庫（BigQuery）等。

### 💡 核心管理特性：權限繼承

GCP 的資源結構遵循 **「上層決定下層」** 的繼承原則。

如果你在「資料夾」層級給了某位同學「檢視者」的權限，那麼該資料夾底下的所有「專案」以及專案內所有的「VM 資源」，該同學通通都能直接檢視，不需要一個一個單獨設定。

# GCP IAM

GCP 的 **IAM（Identity and Access Management，身分與存取管理）**，簡單來說就是雲端世界的「門禁安全系統」。

它的核心任務就是回答一個問題：

> **「誰（Who）」** 可以對哪個 **「資源（Which Resource）」** 進行什麼 **「操作（What Action）」**？
> 

在 GCP IAM 的架構中，這個問題被拆解為三個核心要素（Who、What、How），並透過「**IAM 政策（Policy）**」將它們綁定在一起。

## 🛠️ GCP IAM 的三大核心要素

### 1. 主體 (Who) —— 門禁卡持有者

也就是發出請求的人或程式，在 GCP 中主要分為以下幾種：

- **Google 帳號 (Google Account)：** 一般的個人 Gmail（如 `user@gmail.com`）。
- **G Suite / Google Workspace 帳號：** 企業或學校的專屬帳號（如 `teacher@edu.tw`）。
- **Google 群組 (Google Groups)：** 把多個帳號打包成一個群組，方便一次調整權限。
- **服務帳戶 (Service Account)：** **這非常重要！** 它是專門給「程式、虛擬機（GCE）或自動化腳本」使用的虛擬身分。例如：你的 Flask 程式如果需要讀取 GCP 的資料庫，就必須讓該虛擬機掛載一個擁有權限的 Service Account。

### 2. 角色 (What) —— 權限的集合包

在 GCP 中，你不能直接把單一權限（例如：`允許刪除VM`）直接丟給某個人，必須透過「角色（Role）」來發放。角色可以分為三大類：

- **基本角色 (Primitive Roles)：** 最傳統的粗放權限，分為 **Owner（擁有者，權限最大）**、**Editor（編輯者，可修改資源）**、**Viewer（檢視者，只能看不能改）**。實務上因為權限太大，不符合資安原則。
- **預定義角色 (Predefined Roles)：** Google 針對特定服務量身打造的精細角色。例如：`Compute Viewer`（只能看 VM 資訊，不能開關機）或 `Storage Admin`（可以管理雲端硬碟的檔案）。
- **自訂角色 (Custom Roles)：** 如果 Google 提供的都不合用，你可以自己挑選權限，組裝出一個專屬的角色。

### 3. 資源 (Which) —— 受保護的財產

就是你想控管的對象，例如：某個特定的 Project、某一台 GCE 虛擬機，或是某一個 Cloud Storage 儲存桶。

## 💡 關鍵進階觀念：層級繼承與最少權限原則

1. **權限會由上而下繼承：**
    
    如同前面提到的資源結構，如果你在「組織」或「資料夾」層級賦予某人某個角色，該權限會一路向下傳遞到其下的所有「專案」與「資源」，且**在底層無法被剝奪**。因此，發放上層權限時必須極度謹慎。
    
2. **最少權限原則 (Principle of Least Privilege)：**
    
    這是雲端資安的黃金法則。永遠只給予使用者「完成工作所需要的最少權限」。例如：同學只需要看 VM 狀態，就給 `Compute Viewer` 即可，絕對不要為了圖方便直接給 `Editor` 或 `Owner`。
    

# Metadata是什麼

在雲端運算（特別是 GCP 和 AWS）的領域中，**Metadata（元數據／中設數據）** 的定義非常簡單：**「關於資料的資料」（Data about data）**。

在虛擬機（GCE 或 EC2）的 context 底下，Metadata 就是這台虛擬機的**身分證與設定設定檔**。它記錄了這台虛擬機所有的非硬體規格資訊，供虛擬機內部的作業系統、應用程式或自動化腳本隨時讀取。

## 🏗️ GCP Metadata 的兩大核心分類

當你進入一台 GCP 虛擬機時，Metadata 主要分為以下兩種類型：

### 1. 系統預設 Metadata (Default Metadata)

這是 GCP 自動幫你產生並維護的資訊，只要虛擬機開機，就能直接透過內部 API 查到。

- **常見內容：** 虛擬機的內部與外部 IP、專案 ID（Project ID）、主機名稱（Hostname）、所在的區域（Zone，如 `asia-east1-a`）、目前掛載的 Service Account 權限等。
- **用途：** 讓關在虛擬機裡面的程式「有感」自己身處在雲端的哪個位置。

### 2. 自訂 Metadata (Custom Metadata) —— 課堂實作重點

這就是你剛剛在學校課堂上練習的區塊。你可以自己定義一組 **Key（鍵）** 與 **Value（值）**。

- **常見內容：** 像是你剛剛設定的 `METADATA_DEMO : jian`，或是系統內建最常見的 Key 叫做 **`startup-script`**（沒錯，你的自動化開機腳本，本質上就是一條 Custom Metadata！）。
- **用途：** 用來當作控制虛擬機行為的「開關」或「參數」。

## 🎯 Metadata 到底能拿來幹嘛？（核心應用場景）

### 1. 實現真正的「基礎設施即代碼」（Infrastructure as Code）

就像你今天做的實驗：**同樣一份 Docker 部署腳本，丟到不同的虛擬機上，怎麼讓它跑出不同的結果？**

答案就是利用 Custom Metadata。

- 你可以把環境變數（如 `DB_USER=test`、`APP_ENV=dev`）寫在 Metadata 裡。
- 虛擬機開機時，腳本先用 `curl` 去抓 Metadata，再根據抓到的值去跑對應的設定。這樣你就不需要為了解決微小的環境差異，而手動進去每一台 VM 修改程式碼。

### 2. 服務帳戶（Service Account）的安全權限傳遞

在雲端資安中，我們強調「絕對不要把 AWS/GCP 的密碼或 Access Key 直接寫死在程式碼裡」。

- **標準做法：** 讓虛擬機掛載一個 Service Account，GCP 就會把這個身分權限的 **臨時 Token** 放在 Metadata 伺服器裡。
- 當你的 Flask 程式需要呼叫 GCP 的其他服務（例如讀取 Cloud Storage）時，程式會自動去向內部的 Metadata 伺服器索取臨時 Token。這個 Token 每隔一段時間就會自動更新，既安全又不需要人工介入管理。

## 🔒 重溫安全守則：GCP 的特殊門禁（Header）

為了防止網路攻擊者利用程式漏洞，從外部誘騙你的伺服器去存取內部敏感資料（這種攻擊叫 SSRF），Google 設了一道鐵律：

當你在 VM 內部使用 `curl` 存取 Metadata 伺服器（`http://metadata.google.internal/`）時，**必須強制加上這行 Header：**

Bash

```
-H "Metadata-Flavor: Google"
```

如果沒有加，Metadata 伺服器會認為這個請求不是從合法的內部程序發出的，會直接回傳 `403 Forbidden` 拒絕訪問。

### 📝 總結

> 「GCP Metadata 伺服器是虛擬機與雲端環境溝通的橋樑。它不僅提供了虛擬機的網路與專案基礎資訊，更讓工程師能透過自訂 Key-Value 與 Startup Script，實現動態、靈活且安全的自動化部署。」
>