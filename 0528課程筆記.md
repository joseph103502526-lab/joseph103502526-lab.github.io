# 0528課程筆記

建立時間: 2026年5月28日 下午3:48
更新時間: 2026年5月28日 下午7:21
有實作: No

# AWS_S3_萬用的資料儲存與網頁素材儲存服務

## 理論理解1：S3的應用場景，並了解 Object, Bucket 分別代表什麼意思

Amazon S3（Simple Storage Service）是 AWS 提供的物件儲存（Object Storage）服務，主要用途是用來存放大量非結構化資料，例如圖片、影片、備份檔案、網站靜態頁面、日誌（log）以及資料分析檔案等。它的特點是高耐久性、高擴展性以及按使用量付費，因此在雲端架構中非常常見。

S3 的應用場景非常廣泛。首先，最常見的是檔案儲存與備份。例如企業可以把公司文件、資料庫備份、VM image 或 log 檔放到 S3，因為 S3 可以提供極高的資料耐久性。AWS 官方甚至設計成「11 個 9」的 durability，也就是資料幾乎不會遺失。

另一個常見用途是網站靜態內容託管（Static Website Hosting）。如果網站只有 HTML、CSS、JavaScript 與圖片，而不需要後端伺服器，就可以直接把網站檔案放到 S3，由 S3 提供靜態網站服務。很多前端網站、文件網站甚至 React/Vue build 後的檔案都會放在 S3。

此外，S3 也經常用於大數據與資料分析。例如 AWS Athena、EMR、Glue 等服務，都可以直接讀取 S3 裡的資料。許多公司會把 log、CSV、Parquet 等分析資料集中放在 S3，再交給分析服務處理。

在 DevOps 與雲端架構中，S3 也常被拿來存放 CI/CD artifact，例如：

- Docker build output
- Terraform state file
- application package
- backup script output

因為它便宜、穩定，而且可以很容易和 IAM 權限控制整合。

理解 S3 時，最重要的兩個概念就是 Bucket 與 Object。

Bucket 可以理解成「儲存空間的容器」或「資料夾的最高層級」。它有點像 Linux 中的一個頂層目錄，但實際上更接近「獨立儲存空間」。所有放進 S3 的資料，都必須先放到某個 Bucket 裡。

例如：

```
my-company-backup
student-photo
web-static-files
```

這些都可能是 Bucket 名稱。

Bucket 有幾個特點：

- 名稱必須全球唯一
- 可以設定權限
- 可以設定版本控制（Versioning）
- 可以設定生命週期（Lifecycle）
- 可以指定 Region

因此 Bucket 不只是資料夾，它其實還帶有很多儲存策略與安全設定。

而 Object 則是真正存放的檔案資料。

在 S3 中，所有資料都是以 Object 形式存在。每個 Object 通常包含：

- 檔案內容（data）
- Metadata（描述資訊）
- Key（檔案路徑/名稱）

例如：

```
Bucket: my-photo
Object: cat.jpg
```

或者：

```
Bucket: company-backup
Object: 2026/05/db-backup.sql
```

其中：

```
2026/05/db-backup.sql
```

就是 Object Key。

要注意的是，S3 本質上其實沒有真正的「資料夾」概念。你在 AWS Console 看到的資料夾，實際上只是 Object Key 中的 `/` 被模擬成資料夾結構。

例如：

```
logs/2026/app.log
```

其實是一個完整的 Object Key，而不是 Linux 那種真正的階層式檔案系統。

這也是為什麼 S3 被稱為：

Object Storage（物件儲存）

而不是：

File System（檔案系統）。

簡單來說：

- Bucket = 存放 Object 的容器
- Object = 真正的檔案資料

而 S3 則是一種高可用、高擴展的雲端物件儲存服務。

## 理論理解2:有其它公司的人想要這張圖片，但他沒有你們的aws帳號，你知道aws s3  似乎可以調整檔案的權限，並分享連結給用戶，請先查詢 block public access， acl， bucket policy 分別是什麼意思

在 AWS S3 中，如果想把檔案分享給「沒有 AWS 帳號的人」，本質上就是要讓外部使用者能透過 HTTP/HTTPS 存取某個 Object。因此 AWS 提供了多層權限控制機制，而你提到的：

- Block Public Access
- ACL
- Bucket Policy

就是 S3 權限管理中最核心的三個概念。

首先要理解一件事：AWS 對 S3 的安全策略其實非常保守。因為歷史上太多公司曾經把 S3 Bucket 設成公開，結果造成資料外洩，所以 AWS 後來新增了很多「預設阻止公開存取」的保護機制。

因此現在即使你設定了公開權限，也可能因為更上層的保護機制而無效。

最上層、也是最重要的一層，就是：

## Block Public Access

Block Public Access 可以翻譯成：

「阻止公開存取」。

它是 AWS 後來加入的一個全域保護機制，用來避免使用者不小心把 Bucket 或 Object 公開到網際網路。

它可以套用在：

- 整個 AWS Account
- 單一 Bucket

當它開啟時，即使你：

- 設定 ACL 公開
- 設定 Bucket Policy 公開

AWS 仍然會拒絕公開存取。

也就是說：

Block Public Access 的優先級非常高。

它有點像 Linux 裡的：

「防火牆最終規則」。

即使下面 application 想開 port，防火牆還是能直接擋掉。

因此很多初學者會遇到：

「我明明設定 public read，為什麼還是 Access Denied？」

原因通常就是：

Block Public Access 還開著。

AWS 之所以這樣設計，是因為企業環境中：

「不小心公開」比「不小心鎖太死」危險太多。

接著是 ACL。

## ACL（Access Control List）

ACL 是 S3 早期的權限控制方式。

它可以設定：

- 哪個 AWS 帳號可存取
- 是否允許 public read
- 是否允許 write

ACL 可以套用在：

- Bucket
- Object

例如：

你可以把某張圖片 Object 設成：

```
public-read
```

那任何人只要拿到 URL 都能下載。

這就是最早期 S3 分享圖片的方法。

但現在 AWS 官方其實已經比較不推薦 ACL。

原因是：

ACL 太細碎、太難管理。

因為：

- Bucket 可以有 ACL
- Object 又可以有 ACL
- 不同 object 還能不同設定

大型企業很容易混亂。

所以現在 AWS 越來越偏向：

「關閉 ACL，改用 Bucket Policy + IAM Policy」。

甚至現在建立新 Bucket 時，AWS 常預設：

```
ACLs disabled
```

這代表 AWS 也認為 ACL 已經偏舊。

最後是最重要、現代 AWS 最常用的：

## Bucket Policy

Bucket Policy 是一種 JSON 格式的權限政策。

它作用在整個 Bucket。

你可以理解成：

「S3 的 resource-based policy」。

它很像：

- IAM Policy
- Linux firewall rule
- Kubernetes RBAC

都是用 JSON 規則描述：

「誰可以做什麼」。

例如：

你可以寫：

```json
{
  "Effect": "Allow",
  "Principal": "*",
  "Action": "s3:GetObject",
  "Resource": "arn:aws:s3:::mybucket/*"
}
```

意思是：

任何人都能讀取這個 bucket 裡的 object。

其中：

```
Principal: *
```

代表任何人。

```
s3:GetObject
```

代表允許下載 object。

Bucket Policy 最大優勢是：

集中管理。

你不用對每個 object 設 ACL。

只要一條 policy，就能控制整個 bucket。

這也是現在企業真正主流的方式。

實際上，現在 AWS S3 權限常見架構是：

```
Block Public Access
        ↓
Bucket Policy
        ↓
IAM Policy
```

而 ACL 已經慢慢淡出。

最後，如果回到你題目的情境：

> 有外部公司的人想看圖片，但沒有 AWS 帳號
> 

實務上通常會有兩種做法。

第一種：

把 object 設成 public read。

這種最簡單。

外部人只要：

```
https://bucket-name.s3.amazonaws.com/test.jpg
```

就能下載。

但缺點是：

全世界都能看。

第二種，也是企業更常用的：

Pre-signed URL（預簽名連結）。

AWS 會產生一個：

「有時效性的特殊 URL」。

例如：

- 1 小時後失效
- 只能下載一次

這樣：

外部人不用 AWS 帳號，

但又不用把 Bucket 永久公開。

這在企業裡比 public bucket 安全得多。

# 最終實作總結

結合了**容器化、雲端架構、資安規範與 CI/CD 工作流**的職場概念

# 🚀 AWS 雲端部署與 Docker 容器化實戰報告

## 💡 一、 今日核心實戰流程總結

今天的實驗完整模擬了一套從**在地端開發、版本控制、資安防護、容器打包、到雲端正式部署上線**的標準端到端（End-to-End）DevOps 流程，具體操作步驟如下：

### 1. 本地安全開發與功能替換 (Feature Development)

- **平行時空切換**：在 IDE 終端機建立並切換至全新分支 `feature3`，確保新功能的日常改動絕對不影響主線 `main` 的穩定性。
- **S3 檔案管理實作**：將原本首頁的「晚上活動」按鈕全面重構，引入 Python `boto3` SDK 開發全新的 **AWS S3 雲端檔案上傳與下載管理頁面 (`/storage`)**。
- **【關鍵資安防護】**：程式碼中**完全不出現（硬編碼）任何 AWS Credentials (Access Key / Secret Key)**。Flask 程式直接初始化 `boto3.client('s3')`，預設自動向系統底層撈取憑證，徹底根絕金鑰洩漏至公開程式庫（如 GitHub）而被黑客掃描挖礦的重大資安風險。

### 2. 團隊協作與程式碼審核 (GitFlow Workflow)

- **本地與雲端同步**：將完成測試的程式碼提交（Commit）後推送到 GitHub 的 `feature3` 遠端分支。
- **Pull Request (PR) 審核機制**：在 GitHub 網頁端發起 **Compare & pull request**，模擬企業內部將代碼提交給架構師/主管審核的標準程序。
- **主線合併 (Code Merge)**：模擬主管審核通過後點擊 **Merge pull request**，將功能正式併入 `main` 主線，雲端只留下一個乾淨、穩定且包含所有最新進度（Feature 1, 2, 3）的 `main` 分支。

### 3. 自動化容器封裝與倉庫推送 (Containerization)

- **同步最新主線**：回到本地切換至 `main` 分支並執行 `git pull`，確保本地代碼與雲端審核通過的版本完全一致。
- **Docker 鏡像打包**：根據專案根目錄下的 `Dockerfile`，將最新版 Flask 程式、環境依賴項（補上 `boto3` 的 `requirements.txt`）打包成高規格、輕量化的 Docker 映像檔。
- **推送至 Hub**：執行 `docker push` 將完美封裝的映像檔送至 **Docker Hub 雲端映像檔倉庫 (`cody20431/aws-flask-app:latest`)** 備用。

### 4. AWS 雲端基礎架構建置 (Cloud Infrastructure)

- **EC2 虛擬機架設**：在 AWS 管理主控台（台北機房）啟動一台 **Ubuntu Linux (t2.micro)** 虛擬伺服器。
- **防火牆網路配置**：在 Security Group（安全組）中加開 **`19191` 連接埠（Port）**，允許外網流量正常訪問 Flask 服務。
- **S3 儲存桶創建**：在台北機房建立符合命名規範的 **S3 General Bucket**（例如 ckc101-05），作為網站的持久化靜態資源儲存庫。

### 5. 遠端雲端部署與動態授權 (Production Deployment)

- **SSH 遠端連線**：使用下載的 `.pem` 安全金鑰，透過 Git Bash 的 OpenSSH 穿透連線進入 AWS 雲端 Linux 伺服器內部。
- **環境建置與清理**：在 Linux 上安裝 Docker 引擎，並強制停用與刪除舊版霸佔 Port 的容器（避免 Port 衝突）。
- **【最高核心：IAM Role 設置】**：在 AWS 主控台上建立一個具備 `AmazonS3FullAccess` 權限的 **IAM Role（角色）**，並將其附加（Attach）到該台 EC2 實例上。
- **鏡像拉取與最終上線**：在虛擬機內執行 `docker pull` 拉取 Docker Hub 最新鏡像，隨後執行 `docker run` 啟動容器。因為有了 IAM Role，容器能自動、安全地從 EC2 宿主機繼承 S3 讀寫憑證，網頁完美上線（我還請遠處的朋友嘗試連線網頁並上傳圖片，過程順暢並可在aws bucket看到他們上傳的圖片）。

## 🏢 二、 對齊真實職場的開發部署流程 (Enterprise Mapping)

今天在課堂上的碎步操作，在真實工程業界中其實分別對應了三大 DevOps 核心領域：**CI/CD 持續整合與部署、雲端原生服務（Cloud Native）、以及容器整合技術**。

| **課堂實驗操作步驟** | **真實職場企業開發部署流程對照** | **核心技術領域** |
| --- | --- | --- |
| 開立 `feature3` 分支開發新按鈕與 S3 功能 | 開發人員在 **本地沙盒環境（Local Sandbox）** 進行特徵功能開發。 | **持續整合 (CI)** |
| 程式碼不寫死金鑰，靠環境變數或 `aws configure` 讀取 | 遵循業界資安最高準則 **「憑證與代碼分離 (Separation of Secrets and Code)」**。防止密鑰進入 Git 歷史紀錄（Secret Leakage 防禦）。 | **雲端安全 (Cloud Security)** |
| 在 GitHub 發起 Pull Request 並點擊 Merge | 企業內部的 **Code Review（代碼審查）** 與 **品質閘門（Quality Gate）**。在 CI/CD 中通常會自動觸發靜態代碼掃描（如 SonarQube）與自動化單元測試。 | **持續整合 (CI)** |
| 撰寫 `Dockerfile` 並 `docker push` 到 Docker Hub | **應用程式容器化（Containerization）**。在職場中，通常由 CI 伺服器（如 Jenkins、GitHub Actions）自動將代碼打包，並推送到企業私有倉庫（如 AWS ECR）。 | **容器整合 (Container)** |
| 在 AWS 上創建 EC2 伺服器與 S3 儲存桶 | 基礎架構建置。在成熟團隊中，這步會寫成 **IaC（基礎架構即程式碼，如 Terraform）** 自動化秒級開通。 | **雲端服務 (Cloud)** |
| **建立 IAM Role 並附加至 EC2 實例上** | 職場中嚴格遵循 **「最小權限原則 (Principle of Least Privilege)」** 與 **免金鑰化憑證管理（Credential-less Auth）**。 | **雲端資安 (IAM)** |
| 連線進 EC2 執行 `docker run` 讓網站正式對外運行 | **持續部署 (CD)** 與 **生產環境上線（Production Release）**。透過容器確保了「在我的電腦能跑，在雲端也一定能跑」的環境一致性。 | **持續部署 (CD)** |

## 🔑 三、 重中之重：IAM Role 的設置原因與核心價值

在今天的實戰中，**「建立 IAM Role 並附加給 EC2」** 是含金量最高、也是最符合資安架構師思維的操作。

### ❌ 過去的低規做法 (IAM User + Access Key)

在早期或不規範的開發中，工程師常會在 EC2 伺服器裡面再次執行 `aws configure`，把個人的 `Access Key ID` 和 `Secret Access Key` 寫死在伺服器的硬碟檔案中。

- **致命缺點**：一旦伺服器被駭客入侵（虛擬機被攻破），硬碟裡的這組金鑰就會立刻外洩，駭客將取得該權限下整個 AWS 帳號的控制權。

### 業界標準做法 (IAM Role)

我們完全不給 EC2 任何固定的實體金鑰，而是改用 **IAM Role（機器角色機制）**。

1. **動態權限令牌（Temporary Credentials）**：當我們把 IAM Role 貼在 EC2 额頭上時，AWS 底層會自動透過 **IMDS（執行個體中繼資料服務）**，定時每隔幾小時發放一張「動態過期的臨時安全權限貼紙」給這台 EC2。
2. **容器自動繼承**：容器內的 `boto3` SDK 非常聰明，當它發現程式碼裡沒有金鑰時，會自動向 EC2 主機的底層索取這張臨時貼紙。
3. **無痛維護與極高安全性**：因為沒有實體金鑰檔案存在伺服器硬碟裡，**「無金鑰可偷，自然就不會外洩」**。就算未來需要撤銷、升級權限（例如把 `S3FullAccess` 改成只准讀取單一 Bucket），也只需要在 AWS 網頁主控台一鍵修改 Role 即可，EC2 伺服器和 Docker 容器完全不需要重開機或修改程式碼！