# 0527課程筆記

建立時間: 2026年5月27日 下午7:15
更新時間: 2026年5月27日 下午7:45
有實作: No

# DevOps的核心結構與Github基礎

## DevOps的核心結構

這題申論題考的是 **DevSecOps 與 AI 結合**時，團隊各角色的協作與核心邏輯。

在傳統 IT 中，開發、資安、維運各行其道（資訊孤島）；但在 **DevSecOps with AI** 的架構下，**AI 成為全團隊的「數位副駕駛（Copilot）」**，大幅加速了所有人之間的互動。

以下為你整理最精煉、好拿分的**結構化答案**：

## 申論：DevSecOps with AI 的核心角色互動關係

在 AI 驅動的 DevSecOps 架構中，這六大角色的互動關係從傳統的「線性交接」轉變為「以 AI 為核心的實時協同循環」，其核心互動邏輯如下：

### 1. 專案經理 (PM) 與 全團隊

- **互動角色：** 核心控管者與需求發起者。
- **AI 互動：** PM 負責訂定業務目標與時程。**AI 自動將 PM 的需求轉化為技術規格**（如 User Story）。PM 透過 AI 儀表板，實時追蹤從開發到資安漏洞修復的整體進度，打破團隊間的溝通壁壘。

### 2. 架構師 與 開發、資安人員

- **互動角色：** 藍圖設計者。
- **AI 互動：** 架構師設計系統與安全規範。**AI 負責在開發階段（IDE）實時監督**開發人員的代碼是否符合架構師的標準。同時，架構師利用 AI 進行威脅建模（Threat Modeling），與資安人員互動，提早規劃防禦線。

### 3. 開發人員 (Dev) 與 測試、資安人員

- **互動角色：** 代碼實作者（Shift Left / 測試與安全左移）。
- **AI 互動：** * **與測試互動：** 開發人員編寫代碼時，**AI 自動生成單元測試與整合測試腳本**，供測試人員審查。
    - **與資安互動：** 代碼提交時，AI 自動進行 SAST/DAST（靜態/動態安全掃描）。若發現漏洞，**AI 實時提供修復建議給開發人員**，資安人員只需負責最後的政策審查，大幅減少傳統「資安卡開發」的衝突。

### 4. 測試人員 (QA) 與 開發、維運人員

- **互動角色：** 質量守門員。
- **AI 攻擊：** 測試人員利用 **AI 自動執行黑箱/白箱測試與壓力測試**。測試結果由 AI 自動分類並反饋給開發人員修復；同時，測試通過的報告會由 AI 自動觸發 CI/CD 流水線，通知維運人員準備部署。

### 5. 維運人員 (Ops) 與 開發、資安人員

- **互動角色：** 穩定性與自動化部署負責人。
- **AI 互動：** 維運人員編寫 IaC（基礎設施即代碼）。**AI 連續監控生產環境的效能與異常流量**（如 AIOps）。一旦發現系統卡頓或異常，AI 會自動通知開發人員修復代碼，或啟動自動擴展（Auto-scaling）。

### 6. 資安人員 (Sec) 與 全團隊（核心守護者）

- **互動角色：** 安全政策制定者。
- **AI 互動：** 資安人員不再是最後關頭才來審查的「路障」，而是將資安政策注入 AI 規則中。**AI 在全生命週期中（開發、測試、維運）自動執行資安檢查**。資安人員與維運人員共同透過 AI 監控運行時的安全（Runtime Security），實現自動化防禦。

## 💡 總結得分金句

> **「DevSecOps with AI 的互動核心在於『自動化與左移』。AI 作為全團隊的數位大腦，消除了傳統開發、測試、資安與維運之間的溝通時間差，讓安全（Security）與速度（Velocity）不再需要互相妥協。」**
> 

## Github基礎

## 一、 初學者必須認識的 GitHub 核心術語

1. **Repository (簡稱 Repo / 倉庫)：** 就是你在 GitHub 上的「專案資料夾」。你寫的所有原始碼、文件、圖片都會放在這裡。
2. **Clone (複製/下載)：** 把雲端 GitHub 上的專案資料夾，完整下載到你目前的虛擬機或本機電腦中。
3. **Commit (提交/存檔點)：** 這就像是遊戲的「進度存檔」。每次你改完程式碼，就要建立一個 Commit，並寫下這次改了什麼（Commit Message），方便以後隨時「坐時光機」回到這個版本。
4. **Push (推送/上傳)：** 把你在本機電腦（或虛擬機）上做好的「進度存檔 (Commits)」，同步上傳到雲端的 GitHub 倉庫。
5. **Pull (拉取/更新)：** 把雲端 GitHub 上最新、被別人修改過的程式碼，同步下載並合併到你現在的電腦裡。

## 二、 初學者必學的 6 大 Git 核心指令

當你打開命令列（CMD / PowerShell），在開發專案時，這 6 個指令的出場率高達 90%：

### 1. 複製雲端專案到本地

當你看到別人（或老師）在 GitHub 上的專案，要把他下載下來：

Bash

```
git clone <GitHub專案網址>
```

### 2. 檢查目前檔案狀態

在寫程式的過程中，隨時可以用這行指令查看：有哪些檔案被你修改了？有哪些新檔案還沒被記錄？

Bash

```
git status
```

### 3. 把修改過的檔案加入「暫存區」

告訴 Git，等等要把哪些檔案一起打包存檔。

- 如果要指定單一檔案：`git add app.py`
- **最常用（把所有變更過的檔案一口氣全部加入）：**

Bash

```
git add .
```

### 4. 建立本機的「進度存檔點」

正式把剛剛 add 的檔案儲存成一個歷史版本，`-m` 後面要用雙引號寫下這次存檔的說明（方便以後看）。

Bash

```
git commit -m "第一次部署 Flask 程式成功"
```

### 5. 將本機的存檔「推上雲端」

把你在本機做好的 Commit 進度，正式上傳到 GitHub 雲端倉庫（預設分支通常叫 `main` 或 `master`）。

Bash

```
git push origin main
```

### 6. 從雲端「拉取最新進度」

如果老師或同學修改了雲端 GitHub 的程式碼，你輸入這行，就可以把雲端的最新進度同步抓回你的虛擬機：

Bash

```
git pull origin main
```

## 💡 完整的開發基本流程（順序）

在實務開發中，你的指令敲擊順序通常會長這樣：

1. **`git clone ...`**（只有第一次下載專案需要）
2. 修改你的程式碼（例如剛才修改 `app.py` 的 host）
3. **`git status`**（確認有哪些檔案被動到了）
4. **`git add .`**（把所有修改好的檔案打包）
5. **`git commit -m "修正監聽IP為0.0.0.0"`**（建立本機存檔點）
6. **`git push origin main`**（送上 GitHub 雲端備份，完成！）

# AI 寫程式與程式結構

首先，安裝antigravity IDE，再來新建專案資料夾src/, test/

資料夾用途：

## **1. src 資料夾 (Source Code)**

src 是 **Source**（原始碼）的縮寫。

- **用途**：這裡存放的是你專案的**核心業務邏輯**與**主要程式碼**。也就是說，未來要真正運行、部署到 AWS/GCP 雲端平台上的程式，通通都放在這裡。
- **常見內容**：
    - 主要的應用程式邏輯（如 main.py, app.js）。
    - 雲端部署腳本或基礎設施即代碼（IaC，如 Terraform 的 .tf 檔）。
    - 負責處理資料庫、API 串接的模組。

## **2. test 資料夾 (Test Code)**

test（有時也寫作 tests）是**測試碼**資料夾。

- **用途**：這裡專門存放用來**驗證 src 裡面程式碼是否正確**的測試腳本。在 DevOps 的世界裡，「自動化測試」是核心靈魂。當你修改了 src 的程式碼，你會執行 test 裡的腳本，確保新改的功能沒有把舊的功能改壞（這稱為迴歸測試）。
- **常見內容**：
    - **單元測試 (Unit Tests)**：測試單一函數或功能是否正確。
    - **整合測試 (Integration Tests)**：測試程式與雲端服務（如 AWS S3、GCP BigQuery）串接時是否正常。
- **特色**：這裡的程式碼**不會**被部署到生產環境（Production）中，它只在開發階段或 CI/CD 流水線（Pipeline）跑自動化測試時才會執行。

## **為什麼要這樣分開？**

在 DevOps 的 **CI/CD（持續整合與持續部署）** 流程中，這種結構能讓自動化工具非常有效率地運作：

1. **自動化測試**：CI 工具（如 GitHub Actions、GitLab CI）會直接去讀取 test 資料夾，把裡面的測試全部跑過一遍。
2. **自動化部署**：當測試過關後，CD 工具只會把 src 資料夾裡面的核心程式打包、上傳到 AWS 或 GCP，而不會把測試用的垃圾資料或腳本一起帶到雲端上，進而節省空間並確保安全。

簡單來說：**src 是做正事的，test 是負責監督 src 有沒有做錯事的。**

# AWS-IAM_身分管理服務

**AWS IAM (Identity and Access Management)** 是 AWS 的身分與存取管理服務。它的核心功能是控制「誰（Who）」**可以存取你 AWS 帳戶裡的**「什麼資源（What）」**，以及擁有**「什麼權限（How）」。

用最簡單的物理世界來比喻，它主要管理四個東西：

- **使用者 (User)：** 獨立的個人帳號（例如你建的 `admin-user` 或 `developer`）。就像是發給個別員工的辦公室大門鑰匙與密碼。
- **群組 (Group)：** 使用者的集合（例如 `admin` 部門或 `develop` 部門）。方便你把多個員工分組，一次調整整組人的權限，不用一個一個設定。
- **角色 (Role)：** 臨時的身份（就像我們設定給 EC2 用的）。它沒有固定密碼，像是「臨時員工識別證」，專門讓 AWS 服務之間（例如 EC2 去讀取 S3）可以安全地互相呼叫，或者給外部人員臨時借用。
- **政策 (Policy)：** 用 JSON 格式寫成的「權限說明書」。裡面精準記錄了允許或拒絕什麼行為。你可以把這張說明書貼在 User、Group 或 Role 上面。

> 💡 **金句總結：** > 為了帳戶安全，**絕對不要**直接拿 Root Account（最高權限的創始人帳號）來做日常開發。標準作法是透過 **IAM** 建立有限權限的 User 或 Role，落實「最小權限原則」，這就是 IAM 最核心的存在意義。
> 

# AWS_EC2_作為部屬用

## 映像檔與生命週期 (AMIs & Lifecycle)

- 什麼是 Amazon Machine Image (AMI)？它在啟動 EC2 執行個體的過程中扮演什麼角色？
    - 回答：
    
    **Amazon Machine Image (AMI)** 簡單來說就是這台虛擬機的「系統懶人包」**或**「作業系統鏡像範本」。
    
    它裡面完整打包了作業系統（如 Windows Server 或 Linux）、預先裝好的軟體（如你今天裝的 Python、Git）以及相關的設定。
    
    ### 它在啟動 EC2 的過程中扮演什麼角色？
    
    - **開機藍圖：** 每當你點擊「啟動 EC2」時，AWS 必須知道這台機器開機後要長什麼樣子。AMI 就是那張藍圖。
    - **快速複製：** 只要有了這份 AMI 範本，你就可以在幾分鐘內複製出 10 台、甚至 100 台一模一樣配置的虛擬機，不需要每台都重新開機、重新安裝軟體。
- EC2 執行個體的根磁碟區類型（Root Volume Types）主要有哪些？選擇不同的根磁碟區類型對執行個體的啟動時間和持久性有何影響？
    - 回答：
    
    「根磁碟區」就是虛擬機的**系統碟（C 槽或 Linux 的 / 槽）**。AWS 主要提供兩種根磁碟區類型：
    
    ### 主要類型有哪些？
    
    1. **EBS 磁碟區（EBS-backed）：** 系統裝在獨立的網路外接硬碟上。這是目前最主流、預設的選項。
    2. **執行個體儲存（Instance Store-backed）：** 系統裝在虛擬機底層實體主機的內建硬碟上（現已較少直接作為根磁碟區）。

## 執行個體類型與購買模型 (Instance Types & Purchasing Models)

- 請比較 **隨需型 (On-Demand)**、**預留型 (Reserved)** 和 **Spot 執行個體 (Spot Instances)** 三種主要購買模型之間的成本效益與適用情境。
    - 回答：
    
    ### 1. 隨需執行個體 (On-Demand Instances)
    
    - **成本效益：** **無折扣（正價）。** 按秒或按小時付費，不用綁約、不用付自備款，隨開隨用，不用時隨手關機就停止計費。
    - **適用情境：**
        - **臨時測試/短期專案：** 就像你今天的上課實驗、短期開發測試。
        - **無法預測流量的新服務：** 剛上線的網站，不確定會跑多久、流量多大時。
    
    ### 2. 預留執行個體 (Reserved Instances, 簡稱 RI)
    
    - **成本效益：** **高折扣（最高可省 72%）。** 承諾連續使用 1 年或 3 年，可以用「全額預付」、「部分預付」或「不預付」來換取極高的固定折扣。
    - **適用情境：**
        - **長期穩定運行的系統：** 公司的核心官網、企業內部 ERP 系統、全年不關機的正式資料庫。
        - **預算穩定的專案：** 明確知道未來這三年這台機器絕對不會關機。
    
    ### 3. Spot 執行個體 (Spot Instances)
    
    - **成本效益：** **極致折扣（最高可省 90%）。** 這是標下 AWS 機房裡沒人使用的「閒置算力」。缺點是**不保證穩定**：當 AWS 有正價客戶需要機器時，它會在 **2 分鐘前**通知你，然後強制把你的機器收回（俗稱被斷電/回收）。
    - **適用情境：**
        - **容錯能力高、可隨時中斷的工作：** 大數據分析、影片渲染、科學計算、機器學習訓練。
        - **分散式架構的備用節點：** 壞掉一兩台也不會影響整體系統運作的架構。
- **突發效能型 (Burstable Performance)** 執行個體（如 T 系列）與 **GPU 加速型** 執行個體分別適用於哪種工作負載？
    - 回答
    
    這兩種類型的執行個體設計邏輯完全相反：**T 系列（突發效能型）是「小排氣量但帶有渦輪增壓」的節能車**，而 **GPU 加速型則是「賽道專用的頂級超跑」**。
    
    以下為你精煉出它們的適用工作負載與核心差別：
    
    ## 一、 突發效能型執行個體（T 系列，如 t3.micro, t3.medium）
    
    這類型的執行個體專門設計給「平時很閒，但偶爾會突然很忙」的工作負載。
    
    ### 運作原理：CPU 點數（Credits）機制
    
    平時 CPU 使用率低於基準線時，機器會自動儲存「CPU 點數」；當遇到突發流量（例如使用者突然變多）時，機器就會消耗點數，讓 CPU 效能瞬間爆發到 $100\%$ 運作，等流量過去後再進入省電蓄力狀態。
    
    ### 適用工作負載：
    
    - **低流量的網頁/API 伺服器：** 例如你今天架設的小型部落格、企業內部測試網站或 Flask Demo。
    - **開發與測試環境（Dev/Test env）：** 工程師日常寫程式、編譯代碼的環境（平日沒在跑複雜運算，只有按下編譯的那幾秒需要突發效能）。
    - **微服務（Microservices）：** 職責單一、平時負載極低的微型元件。
    - **代碼儲存庫/小型資料庫：** 供內部少數人使用的 Git 伺服器或開發用資料庫。
    
    ## 二、 GPU 加速型執行個體（如 P 系列、G 系列）
    
    這類型的執行個體硬體裡直接插了強大的 **NVIDIA GPU 顯示卡**。專門用來處理 CPU 算不來、但需要「極大規模平行運算」的超級重工作。
    
    ### 運作原理：
    
    CPU 擅長處理複雜的邏輯判斷，但核心數少（例如 2 核、4 核）；GPU 則擁有數千個串流處理器，極度擅長同時處理大量結構相同的數學運算。
    
    ### 適用工作負載：
    
    - **人工智慧與機器學習 (AI / Machine Learning)：** 大語言模型（LLM）的訓練與推理、深度學習模型（Deep Learning）的跑圖與算力。
    - **高階影像渲染與 3D 建模：** 電影特效動畫渲染、3D 地圖重建、雲端電競伺服器、虛擬實境（VR/AR）串流。
    - **高效能科學運算 (HPC)：** 金融風險模擬（蒙地卡羅模擬）、地震數據分析、基因定序解碼、流體力學計算。
    
    ## 快速對照
    
    - **如果你的服務平時都在睡覺，只有特定的時間點會有人連進來：** 請毫不猶豫選擇 **T 系列**，能幫你省下大筆預算。
    - **如果你的服務涉及矩陣運算、3D 算圖、或是要跑 AI 訓練模型：** 請直接選擇 **GPU 加速型**，用時間換取最高的運算效益。

## 網路與位置策略 (Networking & Placement)

- 在 EC2 的佈署策略中，**區域 (Regions)** 與 **可用區 (Zones)** 之間的關係和區別是什麼？為什麼在多個可用區中佈署資源很重要?
    - 回答：
    
    這三個題目涵蓋了 AWS 最底層的全球基礎設施架構與網路核心。以下為你提供最清晰、結構化的解答：
    
    ## 1. 區域 (Regions) 與 可用區 (Availability Zones, AZs)
    
    ### 關係與區別
    
    - **區域 (Region)：** 是 AWS 在**全球地理位置**上的獨立劃分（例如你今天接觸到的**台北**、**東京**或**雪梨**）。每個區域完全獨立，擁有自己獨立的定價、法規和資源池。
    - **可用區 (Availability Zone, 簡稱 AZ)：** 是區域內部的**實體資料中心叢集**。一個區域（Region）內會包含多個（通常至少 3 個）地理上隔離、但透過超高速光纖網路互相連接的可用區。
    
    > 💡 **一句話區別：** 區域管的是「在哪個城市/國家」，可用區管的是「在該城市的哪幾棟獨立資料中心大樓」。
    > 
    
    ### 為什麼在多個可用區（Multi-AZ）中佈署資源很重要？
    
    - **高可用性與容災（High Availability & Fault Tolerance）：** 如果你把兩台網頁伺服器分別開在「東京可用區 A」與「東京可用區 B」，萬一 A 區遭遇停電、水災或地震導致整個資料中心斷線，B 區的伺服器依然能正常運作，網站**完全不會中斷**。這就是雲端架構最核心的「去中心化防禦」邏輯。
- 什麼是 **彈性網路介面卡 (Elastic Network Adapters, ENA)**？它如何影響執行個體的網路效能？
    - 回答：
        
        ### 什麼是 ENA？
        
        **ENA** 是 AWS 專門為虛擬機（EC2）設計的**高效能虛擬網卡**。它採用了 SR-IOV（單根 I/O 虛擬化）技術，讓 EC2 虛擬機可以直接跟底層的實體網卡溝通，繞過傳統虛擬化層的轉換。這種機制在 AWS 中被稱為 **增強型網路 (Enhanced Networking)**。
        
        ### 它如何影響執行個體的網路效能？
        
        1. **極大吞吐量：** 網路頻寬最高可提升至 **100 Gbps 甚至 400 Gbps**（依機器規格而定）。
        2. **超低延遲與高 PPS：** 大幅降低資料傳輸的時間差，每秒能處理的封包數量（Packets Per Second）顯著提升。
        3. **節省 CPU 損耗：** 將網路流量的處理外包給專門的硬體晶片（如 AWS Nitro），釋放 EC2 的 CPU 算力來專心跑你的應用程式。
        4. **降低網路抖動 (Jitter)：** 確保網路速度的穩定性，避免封包延遲忽快忽慢。
- EC2 執行個體的 IP 定址機制（公共 IP、私有 IP、彈性 IP）的使用場景?
    - 回答：
        
        當你在 AWS 建立 EC2 時，會接觸到三種主要的 IP，它們的使用情境截然不同：
        
        ### ① 私有 IP (Private IP)
        
        - **特性：** 內網專用，網際網路無法直接存取。EC2 關機再開機**不會改變**。
        - **使用場景：** * **後端溝通：** 同一個 VPC 內部的伺服器互相通訊（例如 Web 伺服器去呼叫後端 API）。
            - **隱密資源：** 不需要對外公開的資料庫（如 MySQL）或企業內部系統，確保安全且不產生公網流量費用。
        
        ### ② 公共 IP (Public IP)
        
        - **特性：** 外網可存取。但**致命缺點是只要 EC2 關機 (Stop) 再開機，這個 IP 就會改變**。
        - **使用場景：** * **臨時測試與上課實驗：** 就像你今天在課堂上建立機器、部署完 Flask 馬上要連進去看成果。因為是不需要長期營運的臨時環境，重開機變換 IP 也沒關係。
        
        ### ③ 彈性 IP (Elastic IP, EIP)
        
        - **特性：** 你向 AWS 租用的**固定、靜態公共 IP**。它獨立於 EC2 存在，即使 EC2 關機再開、甚至死機，這個 IP 永遠不變。
        - **使用場景：**
            - **正式營運的網站：** 當你的網域（DNS）需要對應到一個永遠固定的 IP 時。
            - **故障轉移 (Failover)：** 當運作中的主機（A機）死機，你可以一秒內把這個彈性 IP 重新綁定到備用主機（B機），外部使用者完全不會察覺。
            - **外部 API 白名單：** 當你的伺服器要串接金流或第三方銀行 API，對方防火牆要求你提供固定 IP 作為白名單時。

## 安全性 (Security)

- 請解釋 **安全群組 (Security Groups)** 和 **金鑰對 (Key Pairs)** 在保護 EC2 執行個體存取方面各自扮演的角色。
    - 回答：
        
        在 AWS 的安全防護體系中，保護 EC2 執行個體（Instance）就像是保護一棟存放機密資料的辦公大樓。安全群組（Security Groups）**與**金鑰對（Key Pairs）分別擔任了這棟大樓的「外圍駐衛警」與「辦公室大門鎖匙」。
        
        一個管「網路能不能通到機器」**，一個管**「進去時能不能通過身分驗證」，兩者相輔相成。
        
        ## 1. 安全群組 (Security Groups) —— 外圍的「虛擬防火牆」
        
        安全群組屬於網路層面（Network Level）的防護，它直接綁定在 EC2 的虛擬網卡（ENI）上，控制哪些網路流量允許進入（Inbound）或離開（Outbound）你的執行個體。
        
        - **扮演角色：** * **狀態化管理（Stateful）：** 如果你允許某個流量進來（例如允許你家 IP 連入），該流量回去時的安全群組規則就會自動放行，不需額外設定出站規則。
            - **預設全阻擋：** 新建的安全群組預設會**拒絕所有外來的入站流量**。你必須主動新增白名單規則。例如：只允許特定 IP 透過 `Port 22` 進行 SSH 連線，或是允許全世界（`0.0.0.0/0`）透過 `Port 80/443` 瀏覽網頁。
        - **安全職責：** 阻止未授權的網路掃描與惡意攻擊。如果攻擊者的 IP 不在你的安全群組白名單內，他的連線在到達 EC2 作業系統之前，就會在 AWS 底層被直接丟棄。
        
        ## 2. 金鑰對 (Key Pairs) —— 進門的「加密通行證」
        
        金鑰對屬於作業系統層面（OS Level）的登入驗證。它採用了非對稱加密技術，由「公開的公鑰（Public Key）」與「你自行保管的私鑰（Private Key，如 `.pem` 或 `.ppk` 檔案）」組成。
        
        - **扮演角色：** * **預先植入：** 當你啟動 EC2 時，AWS 會把**公鑰**自動寫入該虛擬機作業系統的指定安全目錄中。
            - **雙向解密：** 當你透過 SSH（Linux）或 RDP（Windows 解密密碼）準備登入這台 EC2 時，你必須在自己的本機電腦上出示對應的**私鑰**。作業系統比對公私鑰相符後，才允許你登入。
        - **安全職責：** **徹底取代傳統的「帳號密碼」登入。** 因為傳統密碼容易遭受暴力破解（Brute-force attack）或因設定過於簡單而外洩。而金鑰對是一串極其複雜的加密字串，在黑客沒有拿到你實體 `.pem` 檔案的情況下，幾乎不可能破解登入。
        
        ## 💡 兩者結合的實戰情境
        
        當你想從家裡連線進 EC2 部署程式時：
        
        1. **第一關（安全群組）：** AWS 檢查你的家裡 IP 是否符合 `Web-sg` 的 `Port 22/3389` 允許規則。符合，流量才放行進入 EC2。
        2. **第二關（金鑰對）：** 進入 EC2 作業系統後，系統要求你出示電腦裡的私鑰檔案。驗證與內建公鑰匹配成功，你才能正式取得操作權限。

## 儲存與監控 (Storage & Monitoring)

- 請區分 **Amazon EBS 磁碟區 (EBS volumes)** 和 **執行個體儲存 (instance store persistence)** 的主要差異，尤其是在資料持久性和備份方面。
    - 回答：
        
        這兩者是 AWS 儲存架構中最核心的對比。**Amazon EBS** 的設計哲學是「網路外接硬碟」**，追求資料的安全與持久；而 執行個體儲存 (Instance Store) 的設計哲學是**「本地內建硬碟」，追求極致的讀寫速度。
        
        以下針對兩者的主要差異，特別是**資料持久性**與**備份機制**進行深度區分：
        
        ## 1. 資料持久性 (Data Persistence) 的核心差異
        
        這兩者最關鍵的分水嶺，在於「當 EC2 執行個體關機 (Stop) 或底層硬體故障時，資料還在不在？」
        
        ### Amazon EBS (Elastic Block Store)
        
        - **持久性表現：** **極高。** 它的生命週期與 EC2 執行個體是**分離**的。
        - **關鍵情境：**
            - **關機再開機 (Stop/Start) 或重啟 (Reboot)：** 資料**完全不會遺失**。
            - **執行個體終止 (Terminate/刪除)：** 預設情況下，系統碟（Root Volume）會隨著 EC2 刪除而消失（可修改設定保留），但額外掛載的資料碟（Data Volume）會完好保留，並可隨時掛載給其他 EC2 使用。
        
        ### 執行個體儲存 (Instance Store)
        
        - **持久性表現：** **暫時性的 (Ephemeral)。** 它的資料與 EC2 綁定的實體伺服器緊密相依。
        - **關鍵情境：**
            - **重啟 (Reboot)：** 資料會保留。
            - **關機 (Stop) 或 底層故障 (Host Failure)：** **資料會永久遺失且無法救回。** 因為當你執行 Stop 時，AWS 會釋放底層的實體伺服器資源；下次你執行 Start 時，EC2 會在另一台全新的實體伺服器上啟動，原本舊機器上 Instance Store 裡的資料就會被徹底抹除。
        
        ## 2. 備份與容災機制 (Backup & Redundancy)
        
        ### Amazon EBS
        
        - **內建快照備份：** 支援 **EBS 快照 (Snapshot)** 功能。它是增量備份（Incremental），會將資料安全地儲存在擁有 11 個 9（$99.999999999\%$）高可靠性的 Amazon S3 中。你可以輕鬆設定自動排程備份。
        - **底層硬體容災：** 在同一個可用區（AZ）內，EBS 的底層硬體會**自動進行多重複製**。即使某一塊實體硬碟損壞，底層機制也會自動補上，確保資料不遺失。
        
        ### 執行個體儲存 (Instance Store)
        
        - **無原生備份：** AWS **不提供**針對 Instance Store 的快照或自動備份功能。
        - **硬體故障風險：** 既然它是實體硬碟，只要該台實體伺服器的硬碟壞了，資料就直接消失。
        - **如何實現備份/容災？** 你必須在**應用程式層面**自行架構設計（例如：建立 NoSQL/SQL 資料庫叢集，讓多台 EC2 的 Instance Store 實時同步複製資料），或者自行寫腳本定時將重要資料拷貝到外部的 S3 或 EBS 上。

情境實作 （部署ec2的windows機器，流程）：

今天你成功完成了難度非常高、且完全貼近實務的 **Windows EC2 雲端部署**！

從一開始遭遇嚴重的網路延遲與規格卡頓，到果斷決定「遷廠台北」並升級規格，最後成功打通兩道防火牆讓內外網順利連線，整個標準流程與排查心法匯總如下，非常適合直接拿來當作今天的課程精華筆記：

## 步驟一：在距離最近的區域（台北）建立 EC2 執行個體

為了確保遠端桌面（RDP）操作流暢、擺脫雪梨（Sydney）等遠距離區域帶來的網路延遲，我們選擇在**台北區域**重建：

1. **基本設定：** 選擇 **Windows Server** 的作業系統鏡像（AMI）。
2. **規格優化（核心）：** * **Instance Type：** 放棄只有 1GB 記憶體的 `t3.micro`，改選 **`t3.medium`**（2核4G），Windows 系統才跑得動。
    - **Storage（儲存）：** Windows 系統龐大，根磁碟區（Root Volume）必須設定為 **`30 GiB`** 以上，類型選 **gp3**。
    - **File system：** 屬於 Linux 專用的 EFS，Windows 環境直接**維持預設的 None**。
3. **防火牆預開：** 在安全群組（Security Group）勾選允許 **RDP (3389)**、**HTTP (80)**、**HTTPS (443)**，並建立、下載好台北專屬的金鑰對（`.pem`）。

## 步驟二：透過 RDP 遠端桌面成功登入

1. 待 EC2 狀態顯示為綠色 `Running` 後，點擊 **Connect** $\rightarrow$ **RDP client** 分頁。
2. 點擊 **Get password**，上傳剛剛下載的 `.pem` 金鑰檔案，成功**解密出 Windows 的管理者（Administrator）密碼**。
3. 下載 `.rdp` 連線設定檔，打開後輸入解密後的密碼，順利進入 Windows 虛擬機桌面。

## 步驟三：利用瀏覽器快速安裝環境

使用瀏覽器安裝python, git搭建環境

## 步驟四：打通內外雙重防火牆（關鍵通關）

你的 Flask App 預設執行在 **Port 19191**，要讓網頁順利對外公開，必須完成以下三道設定：

### 1. 修改 Python 原始碼（允許外網監聽）

打開專案的 `src/app.py`，確認最底下的啟動代碼務必加上 `host="0.0.0.0"`。這代表允許所有外接網卡接收流量，而非僅限本機內部：

Python

```
if __name__ == "__main__":
    app.run(host="0.0.0.0", port=19191, debug=True)
```

### 2. 解鎖 Windows 內部防火牆

在虛擬機內打開「具有進階安全性的 Windows Defender 防火牆」，在**輸入規則 (Inbound Rules)** 中「新增規則」：選擇 **連接埠 (Port) $\rightarrow$ TCP $\rightarrow$ 特定本機連接埠輸入 `19191` $\rightarrow$ 允許連線**。

### 3. 解鎖 AWS 安全群組（Security Group）

回到你原本電腦的 AWS 控制台，編輯這台 EC2 綁定的安全群組輸入規則（Inbound rules）：新增一條 **自訂 TCP (Custom TCP)、連接埠範圍填 `19191`、來源選 Anywhere-IPv4 (`0.0.0.0/0`)**。

## 步驟五：啟動與成果驗證

1. 在 Windows 虛擬機的終端機輸入 `python src/app.py` 啟動程式。
2. **內網驗證：** 在虛擬機內打開瀏覽器輸入 `http://localhost:19191`，確認看得到網頁（代表程式運作與內部防火牆正常）。
3. **外網驗證（大功告成）：** 在你**原本的實體電腦**打開瀏覽器，輸入：
    
    `http://<台北EC2的公用IPv4位址>:19191`
    
    網頁順利秒速彈出，代表端到端的雲端部署完美成功！