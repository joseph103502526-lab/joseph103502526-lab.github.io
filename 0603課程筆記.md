# 0603課程筆記

建立時間: 2026年6月3日 下午12:32
更新時間: 2026年6月3日 下午6:20
有實作: No

# 📝 課堂筆記：什麼是 MCP（Model Context Protocol）？

## 📌 什麼是 MCP？

**MCP (Model Context Protocol，模型上下文協定)** 是一個由 Anthropic 開發並開源的**開放標準協定**。

簡單來說，它就像是 AI 界的 **「USB 介面」**。

在過去，如果你想讓 AI 連接不同的資料庫、開發工具、或是本地檔案，工程師必須為每一個工具單獨編寫客製化的串接程式碼（API）。而 MCP 的出現，定義了一個統一的標準，讓大語言模型（LLM）能夠透過這個單一標準，無縫且安全地與所有外部資料源和工具進行溝通。

## 🗺️ MCP 的核心架構

MCP 的運作採取經典的 **客戶端-伺服器 (Client-Server)** 架構：

Plaintext

# 

```
[ AI 應用程式 / 編輯器 ] (MCP Client)
         │
         ▼  (透過 MCP 協定統一溝通)
[ MCP 閘道 / 伺服器 ] (MCP Server)
         │
         ├─► [ 本地檔案 / Git 倉庫 ]
         ├─► [ 資料庫 (PostgreSQL/Notion) ]
         └─► [ 外部工具 / 網頁搜尋 API ]
```

- **MCP Client (客戶端)**：指整合了 MCP 協定的 AI 工具或開發環境（例如：Claude Desktop、Cursor 編輯器）。
- **MCP Server (伺服器)**：專門負責把特定資料或服務「翻譯」給 AI 聽的小程式（例如：PostgreSQL MCP Server、Google Drive MCP Server）。

## 🚀 使用 MCP 有什麼好處？（核心優勢）

> 💡 **核心價值：不再需要為每個 AI 應用重複造輪子。**
> 

### 1. 統一開發標準（解耦 AI 與工具）

- **過去**：如果開發者寫了一個從 Notion 抓資料的工具，他必須為 Claude 寫一個版本、為 GPT 寫一個版本、為 Cursor 又寫一個版本。
- **現在 (有 MCP)**：開發者只需要寫一個 **Notion MCP Server**，任何支援 MCP 協定的 AI 應用程式（Client）就都能直接讀取 Notion 的資料。

### 2. 賦予 AI 「即時上下文環境」與本地控制權

MCP 允許 AI 在使用者的授權下，直接讀取本地的電腦環境：

- 自動安全地讀取你的本機程式碼檔案（Local Files）。
- 直接查詢你的本機資料庫、瀏覽 Git 的 Commit 紀錄。
- 讓 AI 真正變成一個理解你當前工作環境的「資深助理」，而不是只能在瀏覽器視窗裡瞎猜。

### 3. 更強大的安全性與隱私控制（Security & Isolation）

- 所有的資料傳輸與工具執行都發生在 MCP 的規範下，使用者可以非常精細地控制「AI 到底能看哪些資料夾」或「能不能執行這條指令」。
- 相較於把所有隱私資料上傳到雲端，MCP 可以讓 AI 透過本地的 MCP Server 直接處理敏感資料，大幅降低資安外洩風險。

### 4. 繁榮的開源生態系

- 由於 MCP 是完全開源的，全球社群已經迅速建立起龐大的 **MCP Server 生態圈**。無論是想讓 AI 連接 Slack、GitHub、Notion、MySQL 還是 Brave 搜尋，都有現成的組件可以隨插即用。

## 📝 課堂心得與總結

MCP 的出現是 AI 轉向 **AI Agent（智能體）時代** 的關鍵里程碑。以前的 AI 像是關在象牙塔裡的智者，知道很多知識，但沒辦法自己查資料或操作工具；而 **MCP 就像是給了 AI 一套標準的「手跟腳」和「通行證」**，讓它能自主、安全地遊走在各種軟體與資料庫之間，大幅釋放了自動化工作流的潛力。


# 📝 課堂筆記：用 Dify 建立工作流與聊天流 AI Agent

## 📌 實驗目標

本實驗旨在利用 **Dify** 平台建置具備智能引導與自動化觸發的 AI Agent。透過聊天流（Chatflow）**與**工作流（Workflow）的結合，針對使用者的意圖進行分類（Intent Classification），並在不同情境下執行特定的自動化任務：

1. **情境一（課程諮詢）**：結合知識庫（Knowledge Base），讀取全台開班資訊 Excel，提供精準的排班建議。
2. **情境二（餐廳訂位）**：利用條件分支與關鍵字識別（電話、姓名、人數），自動引導使用者至外部訂位網站。

## 🗺️ 系統架構與邏輯流程

在 Dify 中，我們透過核心節點的序列串接來達成以下邏輯：

Plaintext

```
[開始 (Start)]
      │
      ▼
[意圖分類器 (Intent Classifier)]
      │
      ├─► 情境一：詢問雲端課程 ──► [知識庫檢索 (Knowledge Retrieval)] ──► [LLM 建議回覆]
      │
      └─► 情境二：餐廳訂位 ───► [參數/關鍵字識別] ─────────────────► [引導至訂位網站]
```

## 🛠️ Dify 實作步驟紀錄

### 💡 階段一：建立聊天流應用與意圖分類

1. 在 Dify 建立一個新的 **Chatflow (聊天流)** 應用。
2. **新增「意圖分類器 (Intent Classifier)」節點**：
    - 串接在「開始」節點後方。
    - **設定 Target 1 (課程諮詢)**：設定觸發關鍵字或意圖描述（例如：*我想上雲端課程、要報名什麼班、有開課嗎*）。
    - **設定 Target 2 (餐廳訂位)**：設定觸發關鍵字或意圖描述（例如：*我想訂位、幫我訂餐廳、預約用餐*）。

### 💡 階段二：情境一實作 — 雲端課程精準推薦（知識庫應用）

當意圖分類器走到「課程諮詢」分支時：

1. **上傳並建立知識庫**：
    - 進入 Dify 的 **Knowledge (知識庫)** 頁面，上傳老師提供的「全台開班資訊 Excel」。
    - 進行文本分段與向量化（Embedding）處理，建立專屬索引。
2. **新增「知識庫檢索 (Knowledge Retrieval)」節點**：
    - 在課程分支後方，加入此檢索節點，並勾選剛剛上傳的開班資訊知識庫。
    - 將使用者的輸入（User Prompt）作為查詢詞（Query）傳入。
3. **新增「LLM (大語言模型)」節點**：
    - 串接檢索節點，並在 System Prompt 中寫入 Prompt 規範：
        
        > 💡 **System Prompt 範例：**
        你是專業的課程諮詢顧問。請根據知識庫內提供的「全台開班資訊 Excel」內容，嚴格比對使用者的需求，並給予最合適的報名班級建議（包含上課地點、開課日期、班別）。
        > 
4. **輸出**：將 LLM 生成的建議直接回覆給使用者。

### 💡 階段三：情境二實作 — 餐廳訂位與關鍵字識別（工作流引導）

當意圖分類器走到「餐廳訂位」分支時：

1. **新增「參數提取 / LLM 識別」節點**：
    - 為了順利導向訂位網站，AI 必須確保拿到完整的訂位資訊。
    - 設定需要識別並提取的**三個核心變數**：
        - `name` (姓名)
        - `phone` (電話)
        - `guests` (人數)
2. **新增「條件分支 (Condition Decision)」節點**：
    - **檢查機制**：判斷上述三個變數是否皆已成功提取（不為空值）。
    - **若資訊不完整（Else）**：由 AI 繼續追問（例如：「好的，請問您的訂位姓名、電話與人數是多少呢？」）。
    - **若資訊已完整（IF）**：觸發下一步的引導動作。
3. **網頁導向與回覆節點**：
    - 當資料齊全後，透過回覆節點傳回自訂的 Markdown 連結或 URL（例如：`[https://your-restaurant-booking-system.com](https://your-restaurant-booking-system.com)`），並將提取出的姓名、電話與人數以參數形式或提示語帶入，指引使用者點擊前往完成最終訂位。

## 📝 課堂心得與重要觀念總結

> 📌 **核心觀念 1：RAG (檢索增強生成) 的威力**
在情境一中，AI 不需要重新訓練，只要透過「知識庫檢索」就能讀懂企業內部的 Excel 開班資料。這證明了企業級 AI Agent 在面對高頻率變動的資料（如開班日期、剩餘名額）時，RAG 是最直覺且低成本的解決方案。
> 

> 📌 **核心觀念 2：結構化邏輯與意圖分類**
一個好的 AI Agent 不能只靠單一提示詞（Prompt）通透全局。透過 Dify 的 Chatflow，我們能像寫程式一樣，先用「意圖分類器」分流，再用「條件分支」檢查變數（姓名、電話、人數），最後精準導向外部網站。這完美的將「生成式 AI 的彈性」與「傳統程式的邏輯控制」結合在一起。
> 

# 📝 課堂筆記：AWS_VPC網路

## 📌 實驗目標

在 AWS 環境中從零開始規劃並佈建一個具備高可用性（High Availability）**與**網路安全隔離（Network Isolation）的公司網路基礎架構。

- **公有子網路 (Public Subnets)**：用來放置需要對外提供服務的元件（如：Load Balancer、NAT Gateway）。
- **私有子網路 (Private Subnets)**：用來放置核心商務邏輯與資料庫，徹底隔離外網，提升安全性。

## 🗺️ 網路架構圖規劃

本實驗於單一 VPC 內劃分出 4 個 Subnets，跨越兩個不同的可用區（AZ）以達到機房層級的容錯與高可用性：

- **VPC 網段 (CIDR)**: `10.0.0.0/16`
    - **AZ-A (可用區 A)**
        - `public-subnet-1a` (`10.0.1.0/24`)
        - `private-subnet-1a` (`10.0.3.0/24`)
    - **AZ-B (可用區 B)**
        - `public-subnet-1b` (`10.0.2.0/24`)
        - `private-subnet-1b` (`10.0.4.0/24`)

## 🛠️ 實作步驟紀錄

### 💡 階段一：建立 VPC 與 Internet Gateway (IGW)

1. **建立 VPC**：定義公司整體的內部網路範圍為 `10.0.0.0/16`。
2. **建立 Internet Gateway**：建立一個名為 `company-igw` 的網際網路閘道器，這是讓 VPC 內部能與外部網際網路溝通的橋樑。
3. **附加 IGW**：在 Actions 選單中選擇 **Attach to VPC**，將 `company-igw` 正式綁定到剛剛建立的公司 VPC。

### 💡 階段二：建置前端公有網路 (Public Subnets)

為了分散風險，我們在兩個不同的機房（AZ）各建一個公有子網：

1. **建立 `public-subnet-1a`**：
    - 可用區選擇：`ap-northeast-1a` (以東京區域為例)
    - 網段配置：`10.0.1.0/24`
2. **建立 `public-subnet-1b`**：
    - 可用區選擇：`ap-northeast-1b`
    - 網段配置：`10.0.2.0/24`
3. **配置公有路由表 (Public Route Table)**：
    - 編輯 VPC 的主路由表（Main Route Table），在 **Routes** 中新增一條外網路由：
        - **Destination**: `0.0.0.0/0` (代表所有的網際網路流量)
        - **Target**: 選擇前面建立的 `company-igw`
    - 確保這兩個 Public Subnets 預設關聯到此路由表。此時，這兩個子網已具備**雙向連外**能力。

### 💡 階段三：建置後端私有網路 (Private Subnets)

依據資安最佳實踐，內網環境「絕對不可以知道 Internet Gateway 的位置」，因此必須切分出獨立的路由表。

1. **建立 `private-subnet-1a`**：
    - 可用區選擇：`ap-northeast-1a`
    - 網段配置：`10.0.3.0/24`
2. **建立 `private-subnet-1b`**：
    - 可用區選擇：`ap-northeast-1b`
    - 網段配置：`10.0.4.0/24`
3. **建立全新的私有路由表 (Private Route Table)**：
    - 建立一個名為 `private-route-table` 的新路由表，**不添加任何指向 IGW 的外網路由 (`0.0.0.0/0`)**。
    - 此表內僅保留預設的本地路由，確保安全性：Plaintext
        
        ```
        Destination: 10.0.0.0/16  ->  Target: local
        ```
        
4. **手動綁定子網路關聯 (Subnet Associations)**：
    - 進入 `private-route-table` 的 **Subnet associations** 分頁。
    - 點選 **Edit subnet associations**，手動勾選 `private-subnet-1a` 與 `private-subnet-1b` 並儲存。
    - **結果**：成功將這兩個子網從原本會連到外網的主路由表「拉出來」，徹底與外網隔離。

## 📝 課堂心得與重要觀念總結

1. **高可用性 (High Availability) 的實踐**：
    
    在建立 Subnet 時，**不能**將所有資源丟在同一個 Availability Zone。透過手動指定 `1a` 與 `1b`，能確保當 AWS 其中一個機房機架或電力出現故障時，另一個機房的資源仍能正常運作，達到自動容錯（Failover）。
    
2. **網路安全隔離 (Network Isolation)**：
    
    區分 Public 與 Private 的關鍵不在於 Subnet 的名字，而是在於它「關聯到哪一個 Route Table」。
    
    - **Public Subnet**：路由表包含 `0.0.0.0/0 -> IGW`。
    - **Private Subnet**：路由表只包含 `VPC-CIDR -> local`，徹底杜絕外網直接攻擊的風險。
